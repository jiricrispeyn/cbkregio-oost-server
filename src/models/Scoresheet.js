const DATE = 'date';
const NAME = 'name';
const SET_NUMBER = 'set_number';
const PAIR = 'pair';
const SCORE = 'score';
const SETPOINTS = 'setpoints';

const SCORESHEET = {
  key0: DATE,
  key1: NAME,
};

const SETS = {
  key0: SET_NUMBER,
  key1: PAIR,
  key2: SCORE,
  key3: SETPOINTS,
};

module.exports = { SCORESHEET, SETS };
